# Construction DNA Truth Ownership — v0.1

**Date:** 2026-04-07
**Authority:** L0_ARMAND_LEFEBVRE
**Status:** INSTALLED

---

## Assertions

### Assertion 1
The Taxonomy Kernel does NOT own material truth. It owns navigation structure only.

### Assertion 2
The Material DNA Kernel does NOT own UI navigation surfaces. It owns deep material truth only.

### Assertion 3
No execution system may mutate either kernel. Execution systems consume kernel truth via contracts.

### Assertion 4
No observational intelligence system may mutate either kernel. Observation is read-only.

### Assertion 5
Projections are derived, not primary truth. Any view rendered from kernel data is a projection. Projections cannot modify the kernel they project from.

### Assertion 6
No two kernels claim the same truth domain. The boundary map is the arbiter of truth ownership.

### Assertion 7
Microdomains within a kernel do not overlap. Each microdomain has exactly one truth claim.

---

## Truth Precedence Chain

```
Universal_Truth_Kernel (apex doctrine)
    │
    ▼
Construction_Kernel (domain umbrella)
    │
    ├── Material DNA Kernel → canonical material truth
    │       │
    │       ▼
    ├── Taxonomy Kernel → navigation projection of material truth
    │       │
    │       ▼
    ├── Construction_Atlas → spatial context projection
    │       │
    │       ▼
    └── Construction_Application_OS → UI rendering (projection only)
```

Truth flows DOWN this chain. It never flows UP.

UI does not create truth.
Atlas does not create truth.
Taxonomy does not create material truth.
Only kernels create truth.

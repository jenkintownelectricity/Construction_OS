"""Graph typing contracts — kernel typing module.

This module defines graph typing schemas only: node types, edge types,
and relation typing rules. Kernel does not own graph instances, does not
execute projection logic, and does not materialize or query graphs.
"""

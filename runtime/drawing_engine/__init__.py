"""
Construction Drawing Engine — Deterministic Drawing Runtime

This package implements the deterministic drawing runtime engine that consumes
governed Construction OS models and emits drawing outputs through a renderer-safe
instruction pipeline.

The runtime is an execution engine, not a source of construction truth.
"""

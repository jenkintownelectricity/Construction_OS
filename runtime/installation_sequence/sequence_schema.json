{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sequence_schema.json",
  "title": "Installation Sequence Schema",
  "description": "Schema for derived installation sequencing. Each sequence step references a canonical detail ID and a specific installation action.",
  "type": "object",
  "required": ["sequence_id", "detail_ref", "steps"],
  "properties": {
    "sequence_id": {
      "type": "string",
      "description": "Unique identifier for this installation sequence."
    },
    "detail_ref": {
      "type": "string",
      "description": "The canonical detail ID this sequence applies to."
    },
    "steps": {
      "type": "array",
      "description": "Ordered list of installation steps.",
      "items": {
        "type": "object",
        "required": ["step_number", "action", "detail_context"],
        "properties": {
          "step_number": {"type": "integer", "minimum": 1},
          "action": {"type": "string", "minLength": 1},
          "detail_context": {"type": "string"},
          "dependencies": {
            "type": "array",
            "items": {"type": "integer"},
            "description": "Step numbers that must complete before this step."
          }
        },
        "additionalProperties": false
      },
      "minItems": 1
    }
  },
  "additionalProperties": false
}
